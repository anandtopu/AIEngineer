# Module 08 — PyTorch Core

## Goals

- Be able to write and debug a training loop
- Understand batches, optimizers, and basic performance tricks

## Practice

- Run `src/deep_learning/pytorch_training_loop_mnist.py`

## Interview prompts

- Why do we call `model.train()` and `model.eval()`?
- Why do we zero gradients?
- What causes `nan` loss?
